{
  "name": "",
  "version": "",
  "private": true,
  "dependencies": {
    "basscss-sass": "4.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "postinstall": "bundle install",
    "start": "bundle exec jekyll serve ./src",
    "build": "bundle exec jekyll build ./src"
  }
}
