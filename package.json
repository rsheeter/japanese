{
  "name": "",
  "version": "",
  "private": true,
  "dependencies": {
    "basscss-sass": "4.0.0",
    "normalize.css": "4.2.0"
  },
  "devDependencies": {},
  "scripts": {
    "postinstall": "bundle install; mv ./node_modules/normalize.css/normalize.css ./node_modules/normalize.css/normalize.scss",
    "start": "bundle exec jekyll serve ./src",
    "build": "bundle exec jekyll build ./src"
  }
}
